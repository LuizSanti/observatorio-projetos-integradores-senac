import SubmeterProjeto from "./Submeter";

// Como SubmeterProjeto já usa useParams, ele detectará o ID da URL automaticamente.
export default function EditarProjeto() {
  return <SubmeterProjeto />;
}